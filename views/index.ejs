<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600&display=swap" rel="stylesheet">
    <style>
        *{
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>
<body>
    <div class="flex justify-center">
        <div class="p-10">
            <h1 class="text-3xl text-indigo-600">Employee List</h1>
            <div class="bg-indigo-600 w-5 h-1 rounded-full"></div>
        </div>
    </div>
    <div class="flex justify-center p-10">
        <form action="/add" method="post" class="flex gap-2 items-center">
            <label class="text-green-600">Name :</label> <input type="text" name="name" required class="border border-green-600 rounded-md pl-2">
            <label class="text-blue-600">Occupation :</label> <input type="text" name="occupation" required class="border border-blue-600 rounded-md pl-2">
            <label class="text-indigo-600">Salary :</label> <input type="number" name="salary" required class="border border-indigo-600 rounded-md pl-2">
            <button type="submit" class="text-white bg-green-600 rounded-md px-5 py-1 shadow-md">Add Employee</button>
          </form>
    </div>
  <div class="flex justify-center">
    <ul>
        <% data.forEach((item) => { %>
          <li class="border p-2 rounded-md shadow-md flex gap-5 my-5">
            <p class="text-zinc-600"><span class="text-yellow-600"><%= item.name %></span> - <span class="text-blue-600"><%= item.occupation %></span> - <span class="text-green-600"><%= item.salary %></span></p>
            <a href="/edit/<%= item.id %>" class="px-5 rounded-md text-white bg-blue-600">Edit</a>
            <a href="/delete/<%= item.id %>" class="px-5 rounded-md text-white bg-rose-600">Delete</a>
          </li>
        <% }); %>
      </ul>
  </div>
</body>
</html>